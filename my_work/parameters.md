# Parameters

*One row per decision your instruction leaves open. Filled in during session 2, with the agent.*

| Parameter | From which phrase | Range | Default | Why this range |
|---|---|---|---|---|
| Triangle count<br>*number of triangles* | "many triangles" | 100–150 | *(not set)* | look the trace full of the screen. |
| Overlap with previous<br>*how a new triangle meets the last one* | "overlaping" | fixed: on a corner, or on part of the previous triangle — not fully covering it | — | *(not yet said)* |
| How many overlaps<br>*2–3 overlap* | "overlaping" | 2–3 | *(not set)* | *(not yet said)* |
| What is random<br>*size and colour* | "at random but in consequences" | fixed: size and colour are the random parts | — | random means the size and colour. |
| How change proceeds<br>*sequential in general, but at random* | "at random but in consequences" | sequential overall; each change can still be at random. **Correlation** of size, colour and heading along the path (slider). | — | sequential can be the how each change can be in sequential general but at random. it's not random can be morecorrelation and overlap more. |
| Overlap amount<br>*how much of the previous triangle is shared* | "overlaping" | 0.20–0.92; **not too overlap** — opens at 0.40 | 0.40 | not too overlap |
| Colour run<br>*one by one or two by two* | "the colour can be changed maybe one by one or two by two" | 1 = new colour every triangle; 2 = two triangles then change | 1 | maybe one by one or two by two like that |
| Triangle size<br>*scale as a fraction of the page* | "random means the size" | 1/100 – 1/10 of the whole page (written 1/10–1/100; 1/10 is the larger end) | *(not set)* | the scale proportion of the whole fraction |
| Path<br>*where the sequence walks* | "the pattern/trace go over across the screen" | wandering path that may turn; starts from the **top-left** corner; **the whole pattern fills the canvas** (drawable page inside the 10 mm margin) | top-left, fill the sheet | a wandering path that may turn, start from the conner. Top-left, any turn is ok. the whole pattern need to be full of the canvas. |
| Trace visibility<br>*the line of the journey* | "it suppose have some trace" | fixed: a rather clear line-trace is drawn | — | trace visible means have a rather clear line-trace |
| Reverse<br>*rotation after the midpoint* | "reverse onece" / "in the middle point maybe" | midpoint = halfway by **count**; then triangles rotate the other way. Still marked *maybe* | *(not set)* | reverse half by count maybe. |
| Palette<br>*from SpongeBob SquarePants* | "seven colour in contrast colour" / “the sponge colour” | **C — character + Patrick.** Body `#FFD040` · Pore `#E8B800` · Shirt `#FAFAFA` · Tie `#CC2800` · Pants `#704214` · Eye `#6BB8D4` · Outline `#1A1A1A` · Patrick Star Pink `#FF808B` (common digital stand-in; Pantone formula not published as hex). Colours may repeat without cap. | C | the sponge means the cartoon The SpongeBob. the colour should come from that. Pick: C. |
| Triangle construction | "triangles" | fixed: free three points | — | triangle kind can be free three points. |
| Fill and outline | *(never mentioned; asked in session)* | fixed: **both** — fill plus a dark outline | both | both |
| Fade | “add some fade effect” | 0–0.85; later triangles more transparent | 0.50 | add some fade effect |
| Composition beat | “add more composition” | every Nth triangle larger; 0 = off; opens at 4 | 4 | add more composition |

**The last column is yours, in your own words.** If you cannot say why, that is a decision you have not actually made yet.

---

## Things the instruction never mentioned at all

*The interesting ones — decisions you did not notice you were making.*

- Starting corner (now: top-left)
- Whether the wander may turn freely (now: any turn is ok)
- Triangle as free three points rather than equilateral
- Fill vs outline (now: **both**)
- Line weight of the “rather clear” trace (still open — a slider in the tool, awaiting your range)
- Ground colour of the page (white sheet; not a colour decision)
- Spacing along the path, if not already covered by 2–3 overlap (handled by corner/part overlap)

---

## The three regions

**Boring** — where changes to the parameters change nothing you care about.

**Surprising** — where it stopped resembling what you imagined but is still a legitimate execution.

**Breaking point** — where it stops being a valid execution of your instruction at all.

---

## What I rejected, and why
